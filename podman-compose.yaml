# VibeShield Security Framework — Podman Compose Orchestration
# All services run rootless — no daemon required

version: '3.8'

services:
  vibeshield-orchestrator:
    image: vibeshield/base:latest
    container_name: vibeshield-orchestrator
    security_opt:
      - no-new-privileges:true
      - label=disable  # For local dev; use SELinux labels in production
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    user: "1000:1000"
    read_only: true
    tmpfs:
      - /tmp:size=100M,mode=1777
    volumes:
      # Rootless podman socket for spawning sandbox containers
      - /run/user/1000/podman/podman.sock:/run/podman/podman.sock:ro
      - ./config:/app/config:ro
    environment:
      - VIBESHIELD_MODE=PRODUCTION
      - VIBESHIELD_LOG_LEVEL=INFO
      - VIBESHIELD_MAX_AGENTS=50
      - VIBESHIELD_SANDBOX_IMAGE=vibeshield/base:latest
    networks:
      - vibeshield-internal
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    ports:
      - "8080:8080"

  escrow-engine:
    image: vibeshield/escrow:latest
    container_name: vibeshield-escrow
    depends_on:
      vibeshield-orchestrator:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
      - label=disable
    cap_drop:
      - ALL
    user: "1000:1000"
    read_only: true
    tmpfs:
      - /tmp:size=50M,mode=1777
      - /data:size=200M,mode=0700
    volumes:
      - ./escrow-data:/app/data:ro
    environment:
      - VIBESHIELD_ESCROW_MODE=PRODUCTION
      - VIBESHIELD_SETTLEMENT_TIMEOUT=3600
      - VIBESHIELD_CONSENSUS_AGENTS=3
    networks:
      - vibeshield-internal
    restart: unless-stopped
    # No external ports — only reachable via vibeshield-internal network

  audit-logger:
    image: vibeshield/base:latest
    container_name: vibeshield-audit
    depends_on:
      - vibeshield-orchestrator
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    user: "1000:1000"
    read_only: true
    tmpfs:
      - /tmp:size=50M,mode=1777
    volumes:
      - ./logs:/app/logs:rw
    environment:
      - VIBESHIELD_AUDIT_MODE=APPEND
      - VIBESHIELD_LOG_RETENTION_DAYS=90
    networks:
      - vibeshield-internal
    restart: unless-stopped
    # No external ports — log aggregation only

networks:
  vibeshield-internal:
    driver: bridge
    internal: true  # No external internet access — default deny
