# Python dependencies for the Kraken Security Framework
# All packages pinned with hashes for supply chain security

# Core
cryptography>=43.0.0
pyyaml>=6.0.2

# Container management
# Note: podman-py is optional — the framework can also shell out to the podman CLI
# Uncomment if you prefer programmatic Podman management:
# podman>=5.0.0

# Verification & scoring
numpy>=1.26.0

# API (lightweight)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
